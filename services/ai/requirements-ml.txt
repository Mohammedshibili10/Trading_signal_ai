# Optional heavy ML stack.
#
#   pip install -r requirements.txt -r requirements-ml.txt
#
# Only needed when ENABLE_FINBERT=1 (transformer sentiment) or when training the
# gradient-boosted scorers. ~2.5 GB installed, which is why it is not in the
# default image — the service runs fully without it, using the finance lexicon
# sentiment model and the deterministic factor engine.

--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.5.1
transformers==4.48.0
tokenizers==0.21.0
safetensors==0.5.2

xgboost==2.1.3
lightgbm==4.5.0

# Reference TA library. Not imported by engine/ — kept for notebook comparison
# when validating our implementations against a third party.
pandas-ta==0.3.14b0
